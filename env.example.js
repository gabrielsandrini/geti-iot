// HydroSense dashboard config — browser equivalent of a .env file.
//   1. Copy this file to `env.js`   (cp env.example.js env.js)
//   2. Paste your real Blynk Auth Token below.
// `env.js` is gitignored, so your token never reaches the repo.
// index.html loads it via <script src="env.js"> and uses window.BLYNK_TOKEN.
window.BLYNK_TOKEN = "YourAuthTokenHere";
